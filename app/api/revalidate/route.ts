import { revalidatePath } from "next/cache"; import { NextRequest,NextResponse } from "next/server";
export async function POST(request:NextRequest){if(request.headers.get("authorization")!==`Bearer ${process.env.SANITY_REVALIDATE_SECRET}`)return NextResponse.json({error:"Unauthorized"},{status:401});const {locale,kind,slug}=await request.json();const paths=kind==="service"?[`/${locale}/services/${slug}`]:kind==="caseStudy"?[`/${locale}/case-studies`,`/${locale}/case-studies/${slug}`]:kind==="insight"?[`/${locale}/insights`,`/${locale}/insights/${slug}`]:[`/${locale}`];paths.forEach((path)=>revalidatePath(path));return NextResponse.json({revalidated:true,paths});}
