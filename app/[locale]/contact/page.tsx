import { notFound } from "next/navigation"; import { LeadForm } from "@/components/lead-form"; import { isLocale } from "@/lib/i18n"; import { localizedMetadata } from "@/lib/metadata";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return isLocale(locale)?localizedMetadata(locale,"/contact",locale==="en"?"Book a growth strategy call":"Agenda una llamada de estrategia",locale==="en"?"Tell us about your e-commerce growth challenge.":"Cuéntanos sobre tu desafío de crecimiento."):{};}
export default async function Contact({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <section className="py-20"><div className="wrap grid lg:grid-cols-[.75fr_1.25fr] gap-16"><div><p className="eyebrow text-[#3157ff]">{locale==="en"?"Growth strategy call":"Llamada de estrategia"}</p><h1 className="headline mt-7">{locale==="en"?"Let’s find the right lever.":"Encontremos la palanca correcta."}</h1><p className="text-xl leading-relaxed mt-8">{locale==="en"?"We’ll review your context and recommend the service that fits—even when CRO is not the right next move.":"Revisaremos tu contexto y recomendaremos el servicio adecuado, incluso si CRO todavía no es el siguiente paso."}</p></div><LeadForm locale={locale}/></div></section>;}
